-- ====== ROBLOX SCRIPT TROUBLESHOOTING TEST ======
-- Teste zuerst dieses Mini-Script um das Problem zu identifizieren

-- Test 1: Grundlegende Verfügbarkeit
print("=== ROBLOX SCRIPT DEBUG TEST ===")
print("1. Script gestartet")

-- Test 2: game Service
if game then
    print("2. game service: OK")
else
    print("2. game service: FEHLER")
    return
end

-- Test 3: Players Service  
local playersService = game:GetService("Players")
if playersService then
    print("3. Players service: OK")
else
    print("3. Players service: FEHLER")
    return
end

-- Test 4: LocalPlayer
local player = playersService.LocalPlayer
if player then
    print("4. LocalPlayer: OK - " .. player.Name)
else
    print("4. LocalPlayer: FEHLER - Das ist kein LocalScript!")
    print("   LÖSUNG: Erstelle ein LocalScript, nicht ein normales Script")
    return
end

-- Test 5: Andere Services
local uis = game:GetService("UserInputService")
local runService = game:GetService("RunService")

if uis then
    print("5. UserInputService: OK")
else
    print("5. UserInputService: FEHLER")
end

if runService then
    print("6. RunService: OK")
else
    print("6. RunService: FEHLER")
end

-- Test 6: Character
local character = player.Character
if character then
    print("7. Character: OK - " .. character.Name)
    
    -- Test 7: HumanoidRootPart
    local hrp = character:FindFirstChild("HumanoidRootPart")
    if hrp then
        print("8. HumanoidRootPart: OK")
    else
        print("8. HumanoidRootPart: Nicht gefunden, warte...")
        character.ChildAdded:Connect(function(child)
            if child.Name == "HumanoidRootPart" then
                print("8. HumanoidRootPart: Jetzt OK")
            end
        end)
    end
else
    print("7. Character: Nicht geladen, warte auf spawn...")
    player.CharacterAdded:Connect(function(char)
        print("7. Character: Jetzt OK - " .. char.Name)
    end)
end

print("=== TEST ABGESCHLOSSEN ===")
print("Falls alle Tests OK sind, kannst du das Haupt-Script verwenden")
print("Falls Fehler auftreten, prüfe:")
print("1. Verwende ein LocalScript (nicht normales Script)")
print("2. Platziere es in StarterPlayerScripts oder StarterPack") 
print("3. Nicht in Workspace oder ServerScriptService")
